{% comment %}
  The subject line of the password reset email.

  Django strips every newline out of whatever this renders to, so the file
  must produce a single line with no leading spaces. That is why the
  comment closes immediately before the text rather than on a line of its
  own: any indentation left in front of the subject would be joined into
  it.

  This file is supplied by the project for the same reason as the body:
  Django's shipped subject template is fine, but keeping both in one place
  means the wording of the pair can be read together.
{% endcomment %}Athenaeum Ikedi: reset your password
